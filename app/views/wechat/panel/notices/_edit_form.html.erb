<%= form_with model: @notice, url: { action: 'update' } do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <% @template.data_keys.each do |key| %>
    <%= f.select "[mappings][#{key}]", options_for_select(@notice.notify_setting) %>
  <% end %>
  <%= f.submit %>
<% end %>
