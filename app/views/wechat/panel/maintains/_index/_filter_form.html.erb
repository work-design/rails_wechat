<%= form_with theme: 'search', model: Crm::Maintain.new, url: { action: params[:action] } do |f| %>
  <div class="field-body">
    <%= f.text_field :external_userid %>
    <%= f.text_field :pending_id %>
    <div class="field is-narrow">
      <%= f.submit %>
      <%= link_to t('.clear'), filter_params(except: [:external_userid, :pending_id]), class: 'button is-light' %>
    </div>
  </div>
<% end %>
