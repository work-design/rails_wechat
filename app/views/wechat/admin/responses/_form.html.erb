<%= render 'shared/error_messages', target: f.object %>
<%= f.text_field :match_value %>
<%= f.check_box :contain %>
<%= f.number_field :expire_seconds, step: 1, placeholder: '2592000' %>
<%= f.select :effective_type, options_for_select(Wechat::Reply.options_i18n(:type), f.object.effective_type) %>
<%= f.select(:request_types, options_for_select(Wechat::Request.options_i18n(:type), f.object.request_types), {}, { multiple: true, data: { controller: 'choice' } })%>
