<%= form_with model: @response, scope: :response, url: { action: 'update' } do |f| %>
  <%= f.select :effective_type, options_for_select(Wechat::Reply.options_i18n(:type), f.object.effective_type) %>
  <%= f.collection_select :effective_id, @replies, :id, :value %>
  <%= f.submit %>
<% end %>
