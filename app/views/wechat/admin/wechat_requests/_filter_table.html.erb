<table class="as-search">
  <tr>
    <th><%= Wechat::Request.human_attribute_name(:type) %></th>
    <td>
      <%= link_to t('.all'), filter_params(except: [:type]), class: active_params(type: '', active: 'button is-info', item: 'button is-light') %>
      <% Wechat::Request.options_i18n(:type).each do |text, value| %>
        <%= link_to text, filter_params(type: value), class: active_params(type: value, active: 'button is-info', item: 'button is-light') %>
      <% end %>
    </td>
  </tr>
</table>
