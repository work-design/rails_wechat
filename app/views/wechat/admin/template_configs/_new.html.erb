<div class="ui modal" id="modal">
  <i class="close icon"></i>
  <div class="ui header">
    <%= t('.new') %>
  </div>

  <div class="content">
    <%= form_with model: @template_config, url: admin_template_configs_path do |f| %>
      <%= render 'shared/error_messages', target: @template_config %>
      <%= f.text_field :tid %>
      <%= f.text_field :description %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
