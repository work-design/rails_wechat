<td>
  <%= model.match_value %>
  <%= image_tag model.qrcode_data_url, class: 'image is-32x32' %>
</td>
<td>
  <p><time data-controller="time"><%= model.created_at.to_formatted_s(:iso8601) %></time></p>
  <p><time data-controller="time" class="<%= model.expire_at && model.expire_at < Time.current ? 'has-text-danger' : 'has-text-success' %>"><%= model.expire_at.to_formatted_s(:iso8601) if model.expire_at %></time></p>
</td>
<td></td>
