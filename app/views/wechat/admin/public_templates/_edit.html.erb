<div class="ui modal" id="modal">
  <i class="close icon"></i>
  <div class="ui header">
    <%= t('.edit') %>
  </div>

  <div class="content">
    <%= form_with model: @public_template, url: admin_public_template_path(@public_template) do |f| %>
      <%= render 'shared/error_messages', target: @public_template %>
      <%= f.fields :template_key_words, on: { wrap_all: false, label: false, placeholder: true }, css: { wrap_input: 'field' } do |ef| %>
        <%= render partial: 'item_form', locals: { ef: ef } %>
      <% end %>
      <%= f.text_field :notifiable_type %>
      <%= f.text_field :code %>
      <%= f.text_field :description %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
