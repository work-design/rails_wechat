<%= link_to new_admin_app_tag_path(@app), class: 'button is-link' do %>
  <i class="fas fa-plus"></i><span><%= t('.new') %></span>
<% end %>
<%= button_to sync_admin_app_tags_path(@app), method: :post, class: 'button is-link' do %>
  <i class="fas fa-sync-alt"></i><span><%= t('.sync') %></span>
<% end %>
